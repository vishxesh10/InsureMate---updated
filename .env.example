# Example environment variables for InsureMate
# Database: use a full SQLAlchemy URL for production (Postgres) or sqlite:/// for local
DATABASE_URL=sqlite:///./insurance_results.db

# Path to the ML model file inside the container or a mounted volume
MODEL_PATH=/app/model.pkl

# Application host/port
HOST=0.0.0.0
PORT=8000

# Optional: set to 'production' in production deployments
ENV=development
